import React, {useEffect} from "react";
export const Enviar = () => {
    const enviar = useEffect(()=>{},[])

    return (
        <button onClick={enviar} className="btn btn-success" type="submit">
            Responder
        </button>
    )
}